import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idatagear.tictactoetoe',
  appName: 'TicTacToeToe',
  webDir: 'dist'
};

export default config;
